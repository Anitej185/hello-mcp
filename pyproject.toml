[project]
name = "hello-mcp"
version = "0.1.0"
description = "A simple MCP server"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "fastmcp>=0.5.0",
    "pydantic>=2.0",
    "uvicorn>=0.30.0"
]